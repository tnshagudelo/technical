# Bug Report Template

> **Instructions:** Open a new issue using this template. Replace every `<REPLACE_*>` placeholder, then remove this line.

---

## Bug ID: `<REPLACE_BUG_ID>`
_Example: BUG-2024-042_

| Field | Value |
|-------|-------|
| **Title** | `<REPLACE_SHORT_DESCRIPTIVE_TITLE>` |
| **Severity** | Critical / High / Medium / Low |
| **Priority** | P1 / P2 / P3 / P4 |
| **Status** | New / In Progress / Resolved / Closed / Won't Fix |
| **Reported by** | `<REPLACE_REPORTER>` |
| **Reported on** | `<REPLACE_DATE>` |
| **Assigned to** | `<REPLACE_ASSIGNEE>` |
| **Environment** | `<REPLACE_ENV>` (e.g. QA, Staging, Production) |
| **Version** | `<REPLACE_VERSION>` |
| **Component** | `<REPLACE_COMPONENT>` |

---

## Description

`<REPLACE_DETAILED_DESCRIPTION>`
_Describe the problem clearly in 2-3 sentences. Mention the impact to the user._

---

## Steps to Reproduce

1. `<REPLACE_STEP_1>`
2. `<REPLACE_STEP_2>`
3. `<REPLACE_STEP_3>`

---

## Expected Behavior

`<REPLACE_EXPECTED_BEHAVIOR>`
_What should have happened?_

---

## Actual Behavior

`<REPLACE_ACTUAL_BEHAVIOR>`
_What actually happened?_

---

## Screenshots / Logs

> Attach screenshots, screen recordings or relevant log snippets here.

```
<REPLACE_LOG_SNIPPET>
```

---

## Root Cause (filled by developer)

`<REPLACE_ROOT_CAUSE>`

---

## Fix Description (filled by developer)

`<REPLACE_FIX_DESCRIPTION>`

---

## Verification Steps (filled by QA after fix)

- [ ] Bug is no longer reproducible following the steps above.
- [ ] Regression tests pass.
- [ ] Related test cases updated if necessary.
